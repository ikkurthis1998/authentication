import { createBusiness } from "./createBusiness";
import { updateBusiness } from "./updateBusiness";
import { getBusiness } from "./getBusiness";

export const businessCRUD = {
    createBusiness,
    updateBusiness,
    getBusiness
}